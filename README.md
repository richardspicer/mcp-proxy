### mcp-proxy

```markdown
# ⚠️ mcp-proxy has moved

**This repository has been deprecated.** mcp-proxy is now part of [counteragent](https://github.com/richardspicer/counteragent), a unified AI agent red team platform.

## Migration

See the [counteragent README](https://github.com/richardspicer/counteragent#readme) for installation instructions.

The CLI command is now:

```bash
counteragent proxy start --transport stdio --target-command "your-server-command"
counteragent proxy replay --session-file session.json
